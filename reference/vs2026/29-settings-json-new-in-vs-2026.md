## Settings JSON (New in VS 2026)

VS 2026 introduces a JSON-based settings file alongside the traditional Tools > Options UI. Every setting change is reflected in this JSON file, which:

- Makes settings version-controllable
- Enables diffing between configurations
- Allows bulk editing of settings
- Supports import/export between machines

Access: The new Settings UI includes a link to open the JSON file directly.

---
